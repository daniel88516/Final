# busybox-meta

BusyBox integration metadata for the diagnostics applets.

- `Config.src` — Kconfig menu, copied into `third_party/busybox/diagnostics/` by `scripts/integrate_busybox.sh`
- `Kbuild.src`  — build rules, same destination
