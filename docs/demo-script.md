# Demo Script

1. Show the repository layout and explain that BusyBox is an external dependency.
2. Run `make init-submodule`.
3. Run `make integrate`.
4. Run `make build`.
5. Run `make test`.
6. Demonstrate `bbfscheck`, `bbtop`, and `bbnetmon` from the integrated BusyBox binary after the applets are enabled.
